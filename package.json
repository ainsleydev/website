{
	"name": "ainsley.dev",
	"version": "1.0.0",
	"author": "Ainsley Clark",
	"description": "Website code for ainsley.dev",
	"license": "BSD-3",
	"main": "index.js",
	"scripts": {
		"setup": "sudo chmod -R 777 ./bin && ./bin/setup-deps.sh && ./bin/setup.sh",
		"setup:deps": "sudo chmod -R 777 ./bin && ./bin/setup-deps.sh",
		"serve": "hugo server",
		"build": "npm run hugo:clean && hugo --theme=ainsley.dev --environment development",
		"build:staging": "npm run hugo:clean && hugo --theme=ainsley.dev --environment staging --config config-staging.yml --minify && npm run image",
		"build:production": "npm run hugo:clean && hugo --theme=ainsley.dev --environment production --config config-production.yml --minify && npm run image",
		"build:prod": "npm run build:production",
		"hugo:clean": "rm -rf public",
		"image": "sudo chmod -R 777 ./bin && ./bin/image-optim.sh",
		"clean": "prettier --config ./config/.prettierrc --write ./**/*.{css,scss,json,js} --ignore-path ./config/.prettierignore && npx eslint -c ./config/.eslintrc assets/js/app.js --fix",
		"lint": "npm run lint:scss && npm run lint:js",
		"lint:scss": "sass-lint --config ./config/.sass-lint.yml '**/*.scss' --verbose --no-exit",
		"lint:js": "npx eslint -c ./config/.eslintrc **/*.js"
	},
	"dependencies": {
		"smoothscroll-polyfill": "^0.4.4",
		"vanilla-lazyload": "^17.8.3"
	},
	"devDependencies": {
		"eslint": "^8.31.0",
		"hugo-bin-extended": "^0.109.0"
	}
}
